apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-html-pvc
  namespace: k8s-lab-01-persistence-unit
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
